# operations on an array 
import numpy as np
def operational_array:
sum=0
product=0
  for i in range(len(array)):
  sum+=i
  for i in range(len(array)):
  product*=i
print("sum="+"str(sum)+","product="+"str(product)")
operational_array=np.array(1,2,3,4,5,6)
operational_array()


   